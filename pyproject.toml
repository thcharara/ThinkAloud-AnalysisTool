[project]
name = "thinkaloud"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
  "pandas", "numpy", "regex", "pydantic", "rich", "matplotlib", "seaborn",
  "scipy", "scikit-learn", "rapidfuzz", "flask"
]
