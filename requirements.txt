# Runtime dependencies for the coding tool + CLI.
# For downstream statistical analysis (pandas, matplotlib, notebooks), see the
# optional "analysis" extra in pyproject.toml:  pip install -e ".[analysis]"
flask
pyyaml
pydantic
typer
rich
unidecode
